# 项目8：单幅图像超分辨率重建实验报告

- 姓名：庞嘉、郭诣丰、朱程炀
- 专业：计算机科学与技术、计算机科学与技术（拔尖人才试验班）
- 代码仓库地址：https://github.com/StevenPang203426/SISR-Team8.git
- 指导老师：丁恒辉
- 实验日期：2026.4-2026.6

感谢助教在实验过程中提供的帮助和指导！

---

## 目录
1. 前情部分
2. 实验方法
3. 实验内容
4. 结果分析
5. 实验分工
6. 收获与感想

---

## 1. 前情部分

本次 PJ8 的主题是单幅图像超分辨率重建，即**从一张低分辨率图像恢复出更清晰的高分辨率图像**。参照助教给出的要求文档，我们认为需求比较明确：即需要**在数据加载阶段实时生成退化图像，完成至少一种深度学习超分辨率模型的实现，并在测试阶段计算 `PSNR`、`SSIM` 等指标，同时与传统的双三次插值方法进行对比**。相比于平时丁老师课上的简单实验，PJ 题目则更接近真实视觉任务，因为图像采集、传输、存储和显示的各个环节都可能受到分辨率限制，而“如何在已有低清图像上尽量恢复细节”这件事，本身就是十分典型的工程需求。

从内容上看，超分辨率任务和我们在课上接触到的卷积神经网络、图像退化与恢复、感受野、特征提取、上采样等知识都有比较直接的联系。表面上仅仅是将图放大，但真正考究细节时，我们发现这并不是简单插值就能解决的问题。因为放大之后那些边缘、纹理和局部高频信息，很多在低分辨率图里已经丢失了，因此模型实际上是**在有限观测下做一个带先验的重建问题**。这也正是计算机视觉中非常常见的一类问题：输入信息是不完整的，我们只能依靠数据分布、模型结构和训练经验去尽量恢复合理结果。

在正式开始 PJ 前，我们小组查阅了一些相关资料。我们逐渐了解到，**单幅图像超分辨率**的发展路径很有代表性。较早的方法主要依赖**插值、稀疏表示和字典学习**，而后来随着卷积神经网络的发展，端到端学习逐渐成为主流。我们这次最终选择 `SRCNN`、`ESPCN` 和 `EDSR` 三种模型作为主线，正是希望沿着“经典浅层 CNN -> 高效亚像素卷积 -> 更深的残差网络”这样一条较清晰的路线来完成。这样一方面便于和论文思想相对应，另一方面也便于最终分析和阐释模型能力、复杂度和工程取舍之间的关系。

---

## 2. 实验方法

### 2.1 总体思路

在本次实验中，我们的宗旨是尽量按照一个完整的超分辨率实验流程来搭建系统：

1. 以 `DIV2K` 高分辨率图像为基础数据；
2. 在数据加载阶段实时使用双三次下采样生成低分辨率图像；
3. 分别训练 `SRCNN`、`ESPCN`、`EDSR` 三类代表性模型；
4. 在验证阶段记录 `loss` 和 `PSNR` 曲线，并保存最优权重；
5. 后续再基于同一套模型接口进行测试和可视化分析。

之所以采用这样的路线，是因为我们看到助教要求中已强调了**退化处理、模型实现、指标计算、局部细节对比、效率分析**这几项内容。若只做单个模型，虽然也能完成基本要求，但难以形成比较完整的分析；若一开始就上很重的模型，又容易把时间耗在算力和调参上。因此，我们最终采用三种结构差异较明显、又都属于超分辨率经典方法的模型作为 baseline。这样既有利于控制工作量，也能够体现我们对方法演化过程的理解。

### 2.2 为什么选择这三种模型？

#### 2.2.1 SRCNN：作为经典起点

`SRCNN` 是深度学习超分辨率中最经典的早期工作之一。阅读原论文后，我们认为它最适合作为本次实验的起点。一方面，`SRCNN` 的结构非常直观，核心思想是**直接学习从低分辨率图像到高分辨率图像的端到端映射**；另一方面，它具有很强的代表性，很多后续方法都可以被看作对它的改进。

在论文调研中，我们发现其对不同卷积核配置做了比较，常见的经典结构可以记为 `9-1-5`。其中第一层用较大的 `9x9` 卷积做特征提取，中间用 `1x1` 卷积做非线性映射，最后用 `5x5` 卷积完成重建。那么，我们在当前项目中就也采用这一结构：`9x9 -> 1x1 -> 5x5`，通道数为 `64 -> 32 -> 3`。如此做的原因也比较朴素：一是它与论文中的经典版本一致，二是其含义清楚、便于分析，三是参数规模仍比较可控。对于 PJ 项目而言，这样的选择比较稳妥。

另外，`SRCNN` 的一个特点是通常先把 LR 图像通过双三次插值放大到目标尺寸，再交给网络去做“细节修正”。从方法史的角度看，这个设计很自然，因为它延续了传统插值放大的处理习惯；但从计算效率看，它也意味着网络在更大的分辨率空间里做卷积，因此速度未必最优。我们小组成员自然看到了这一点。也正因此，后续的 `FSRCNN`、`ESPCN` 中，我们才会把“尽量在 LR 空间中做计算”作为一个核心方向。

#### 2.2.2 ESPCN：作为高效上采样代表

`ESPCN` 的核心贡献在于提出了 **sub-pixel convolution**，也就是我们在代码里实际使用的 `PixelShuffle`。相比 `SRCNN` 先插值、后卷积的思路，`ESPCN` 会先在低分辨率空间提取特征，把主要计算量尽量留在 LR 空间，最后再通过亚像素重排一次性恢复到高分辨率输出。这种设计在计算效率上更自然，也更符合先做特征抽取，最后再上采样的直觉。

从项目角度看，`ESPCN` 非常适合进行分析与讨论。因为它不仅能得到结果，还能够形象地展示上采样层在卷积网络中的作用。在要求中提到了希望我们能理解 `PixelShuffle` 或转置卷积的作用，我们选择 `ESPCN`，正是为了将这一点实现。在目前我们的代码中，`ESPCN` 先用两层卷积提取特征，最后通过 `nn.PixelShuffle(scale)` 将通道维重新组织为空间分辨率，这个过程就非常能说明问题。

我们保留 `ESPCN` 作为第二个 baseline，还有一个现实考虑：其结构不算复杂，但在速度和性能之间往往能取得较好的平衡。现实中很多时候最强性能往往也难以完全实现，更多地我们会追求一个清晰而完整的结果。`ESPCN` 恰好适合承担这样一个角色。

#### 2.2.3 EDSR：作为更强性能基线

在阅读 `EDSR` 相关论文时，我们比较明显地感受到，这一阶段的超分辨率模型已经不再满足于单纯的“能做”，而是在系统追求更高的重建质量。`EDSR` 的核心思想之一，是**在残差网络框架下对超分辨率任务进行更有针对性的改造**，例如去掉不必要的 `Batch Normalization`，并使用残差结构来支持更深的特征建模。在论文中，还讨论了 residual scaling 等细节，目的正是在提高表现的同时保持训练稳定。

结合课程项目的时间与算力限制，我们没有完全照搬论文中的超大模型，而是采用了一个较为简化、但思路一致的 `EDSR` 版本：主体由 `16` 个残差块构成，特征通道数为 `64`，并在尾部使用 `PixelShuffle` 完成上采样。简化版 `EDSR` 仍然保留了“深层残差特征提取 + 最后上采样”的主干结构，这已足以体现方法层面的优势。

从最终日志结果看，这样的选择也是合理的：在三个尺度上，`EDSR` 都取得了当前实验中的最好验证 `PSNR`。这也验证了哪怕是一个简化版本，只要把关键设计保留下来，也能明显优于更浅层的方法。

### 2.3 训练策略

除了模型本身，我们在实验设计上还做了一些比较务实的考虑。

首先，数据退化采用双三次下采样动态生成，而不是预先将所有 LR 图像离线保存。这样做一方面符合要求，另一方面也能让训练流程更清晰：只需要维护 HR 数据集即可，避免不同尺度的 LR 文件反复管理。这样的实现也更能体现我们对于数据对构造方法的理解。

其次，我们统一使用 `500` 个 epoch、`batch size=16`、`patch size=96`、`Adam` 优化器和 `MSE` 损失函数。这样的统一设置便于不同模型之间做横向比较。特别是对于超分辨率任务，`MSE` 对应的 `PSNR` 指标关系较直接，因此用它做 baseline 是自然的。虽然在任务说明中提到了感知损失，但当前 baseline 阶段，我们认为可以暂时先不引入更复杂的感知项，先把像素级重建这条主线做好。

最后，我们保留了 `wandb` 记录和本地 `log` 文件记录两套实验痕迹。实际操作中，网页端的曲线更适合快速查看训练趋势，而本地日志更适合后续分析时直接提取参数和数值。这部分工程在真正整理结果时是很有帮助的。

---

## 3. 实验内容

### 3.1 代码总体结构

本项目的代码结构比较清楚，训练、测试、推理、模型、数据与工具函数都可以分开组织。以下几个部分是系统核心：

1. `train.py`：训练入口，负责读取配置、构建数据集、模型、优化器与损失函数；
2. `test.py`：测试入口，负责加载权重并在指定数据集上进行评估；
3. `data/dataset.py`：数据集构造，完成 HR 图像读取、裁剪增强和实时下采样；
4. `models/`：各类超分辨率模型定义；
5. `core/trainer.py`：完整训练循环，包括日志、验证、学习率调度和保存最优模型；
6. `core/evaluator.py`：验证与测试指标计算；
7. `core/inferencer.py`：面向实际图片的推理接口，支持单图或目录推理；
8. `utils/`：图像缩放、指标、画图、模型复杂度统计等辅助工具。

这样组织的好处是模块边界比较清楚。后续若继续扩展 `FSRCNN`、`IMDN` 或加入新的损失函数，也较容易在现有框架上继续推进。

### 3.2 数据加载与退化构造

`data/dataset.py` 是本次作业要求中最关键的部分。我们没有直接读取预先生成好的低分辨率图，而是从高分辨率图像出发，在 `__getitem__` 中实时构造训练样本。具体过程如下：

1. 从 HR 图像目录中读取原图，并过滤掉损坏图片；
2. 训练阶段对图像做旋转、随机裁剪和翻转增强；
3. 将裁剪后的 HR patch 通过双三次插值下采样得到 LR；
4. 若模型为 `SRCNN`，再先把 LR 图像双三次上采样回目标尺度；
5. 将 LR / HR 对转换为张量，交给网络训练。

这样的处理流程很符合超分辨率任务本身的定义。因为我们手头本就只有 HR 真值，LR 图像实际上是人为模拟出来的退化结果。把这一过程放到 `DataLoader` 内部，不仅满足了作业中实时下采样的要求，也让不同放大倍率下的训练过程更加统一。

另外，实现中统一把图像尺寸调整为 `12` 的倍数，再进行切块或下采样处理。这一做法在多尺度训练中很有必要，因为其可以减少尺寸不整除导致的额外麻烦。实际上我们调研也发现，很多 bug 往往就会出在这些小地方（实际实现后我们对此体会比较深）。

### 3.3 模型注册与三种主模型实现

`models/__init__.py` 中实现了统一注册表，训练和测试阶段都通过 `build_model(name, scale, in_channels)` 来创建模型实例，而不是在各处手动写一遍导入逻辑。由此可让整个项目显得更规整，也方便在同一套框架下比较不同模型。

#### 3.3.1 SRCNN

`models/srcnn.py` 中的 `SRCNN` 采用三层卷积结构：

1. `9x9` 卷积提取较大感受野下的浅层特征；
2. `1x1` 卷积做跨通道映射；
3. `5x5` 卷积做重建输出。

从代码上看，这个实现和论文中的经典 `9-1-5` 结构是对齐的。我们之所以保留这一形式，不是因为它一定最强，而是因为它最适合作为深度学习超分辨率的起点来分析。其逻辑简单，训练也较稳定，能够给后续比较提供可靠参照。

#### 3.3.2 ESPCN

`models/espcn.py` 中的 `ESPCN` 先在 LR 空间里用卷积提取特征，再用最后一层卷积生成 `in_channels * scale^2` 个通道，随后交给 `PixelShuffle(scale)` 重排成 HR 输出。这里的关键点并不在于卷积层数的多少，而是在于上采样逻辑从“预插值”切换成了“网络内重排”，从而实现了更高效的重建。

这一点对理解工程的核心思路很有帮助。我们在写代码和读论文时都明显感觉到，`PixelShuffle` 不只是一个可替换的操作，它其实体现了**整个计算位置的转移**：前面的特征提取都可以留在低分辨率空间里完成，从而降低计算开销。这也是 `ESPCN` 比 `SRCNN` 更高效的主要原因。

#### 3.3.3 EDSR

`models/edsr.py` 实现的是一个简化版 `EDSR`。代码中，每个残差块由两层 `3x3` 卷积和中间的 `ReLU` 组成，并在输出处乘以 `0.1`，再与输入相加；主体部分堆叠了 `16` 个残差块；尾部再利用 `PixelShuffle` 完成上采样。

当前版本，我们并未完全复现原论文的大规模结构，但在方法上确实保留了核心思想：我们通过更深的残差特征抽取来提升重建能力，并把上采样放在尾部统一处理。尤其值得注意的是，当前实现并未使用 `Batch Normalization`，这一点也与论文中的改造方向保持一致。对于课程项目而言，这样简化而不失真的复现方式是合理的。

### 3.4 训练流程设计

训练主流程由 `train.py` 和 `core/trainer.py` 协同完成。整体逻辑如下：

1. 从 YAML 配置文件中读取模型名、训练目录、倍率、学习率等参数；
2. 构建训练集与验证集 `DataLoader`；
3. 根据模型名称创建网络；
4. 设定优化器、损失函数和学习率调度器；
5. 进入 epoch 循环，执行前向传播、反向传播和参数更新；
6. 每个 epoch 后在验证集上计算 `loss` 与 `PSNR`；
7. 若当前验证 `PSNR` 更优，则保存 `best.pt`；
8. 最终保存训练日志和曲线图。

在这套实现里，我们特别关注了两点。

**第一，`SRCNN` 的优化器采用了分组学习率，最后的重建层学习率更小。**这是因为该模型在原始设计中就有类似的训练习惯，当前实现也延续了这个想法。**第二，学习率调度器使用 `ReduceLROnPlateau`，根据验证 `PSNR` 的变化自动衰减学习率，这对 500 epoch 的训练过程比较有帮助。**将这些基础工程处理到位后，训练曲线会更平稳。

### 3.5 验证、测试与推理接口

`core/evaluator.py` 中既实现了训练阶段的快速验证，也实现了测试阶段的完整评估。验证时主要返回平均 `loss` 和 `PSNR(Y)`；测试时则进一步支持 `SSIM`、参数量、`FLOPs`、`FPS` 的统计。实验中，我们还额外导出了 `metrics.png` 和 `.log` 文件，对运行结果进行保留。从代码上看，这一套测试接口是完整的。

同时，我们在 `core/inferencer.py` 中还设计了对单张图片和目录图片进行推理的能力，并包含针对大图的分块推理逻辑。我们的目标并不仅仅是把训练跑通，而是更多地考虑到后续真正拿图像做超分辨率的使用场景。

### 3.6 训练记录与结果整理

在训练完成后，我们为每组实验都保留了三类结果文件：

1. `best.pt`：最佳模型权重；
2. `*.log`：训练参数、每轮指标和最终最优 `PSNR`；
3. `metrics.png`：训练集和验证集的 `loss / PSNR` 曲线图。

在我们看来，这样的保存方式已经足够支撑最终的结果分析。尤其是 `.log` 文件，里面既包含超参数，也包含最终结果，后续查表、整理汇总都比较方便。

---

## 4. 结果分析

### 4.1 训练设置

根据运行后在 `experiments` 目录下得到的日志文件，三种模型在三个放大倍率上的训练设置保持统一：

1. `Epochs = 500`
2. `Batch Size = 16`
3. `Patch Size = 96`
4. `Learning Rate = 1e-4`
5. `Optimizer = Adam`
6. `Criterion = MSE`

这种统一设置使得不同模型的结果更具有可比性。因为如果每个模型都大量单独调参，不排除最后得到的结果可能更好，但很难判断提升究竟来自模型结构本身，还是来自额外的人工干预。

### 4.2 验证集结果汇总

根据 `experiments/*/*.log` 中记录的 `Best PSNR`，当前九组 baseline 的验证结果如下表所示：

| Scale | SRCNN | ESPCN | EDSR |
|---|---:|---:|---:|
| x2 | 32.89 dB | 32.57 dB | 34.22 dB |
| x3 | 29.48 dB | 29.20 dB | 30.54 dB |
| x4 | 27.67 dB | 27.48 dB | 28.56 dB |

从这个结果可以看出两个非常直观的现象。

第一，随着放大倍率从 `x2` 增大到 `x4`，三种模型的 `PSNR` 都明显下降。这是符合直觉的，因为倍率越大，意味着要补的细节越多，问题本身也更难。第二，在当前实验设置下，`EDSR` 在三个尺度上都取得了最好的验证结果，说明更深的残差结构确实在细节恢复上具有优势。

### 4.3 不同模型的表现比较

如果从结果趋势来看，**`SRCNN` 和 `ESPCN` 在本次实验中的表现比较接近，而 `EDSR` 整体更强**。这样的结果与我们在方法部分的预期也基本一致。

`SRCNN` 的优点在于结构简单、容易理解，是很好的基线；但它需要先对 LR 图像做双三次放大，再在 HR 空间做卷积，因此在方法上已经带有一定历史局限。然而，`ESPCN` 在这个基础上引入 `PixelShuffle`，把主要卷积计算留在 LR 空间，这在工程上更自然，也更高效。不过从当前结果看，它在本实验中的验证 `PSNR` 略低于 `SRCNN`，说明速度友好并不必然”自动等于“更强的效果，具体表现仍与网络容量、训练策略和数据设置有关。

当然，`EDSR` 的表现最稳定，这也最符合深层超分辨率网络的经验规律。其优势主要来自更强的特征表达能力和更深的残差主干。这些结果也帮助我们形成了一个比较清楚的认识：**浅层模型更便于说明问题，高效模型更便于讨论上采样机制，而深层残差模型则更适合作为性能基线**。

### 4.4 训练曲线分析

当前每组实验目录下都保存了训练曲线图，图中同时记录了训练集和验证集的 `loss`、`PSNR` 变化。以三个代表性实验为例：

![SRCNN x2 训练曲线](experiments/srcnn_x2/metrics.png)

![ESPCN x2 训练曲线](experiments/espcn_x2/metrics.png)

![EDSR x2 训练曲线](experiments/edsr_x2/metrics.png)

从这些曲线可以看出，大部分模型在训练前期提升较快，后期逐渐趋于平稳。这说明当前训练轮数和学习率调度策略基本是有效的。特别是 `EDSR`，其训练 `PSNR` 和验证 `PSNR` 最终都达到了更高水平，说明更深网络在当前数据和配置下确实学到了更强的映射关系。

若后续还有时间，我们认为还可以继续做更多的补充分析：例如将不同模型在同一尺度下的训练曲线放到一张图中，便于直接比较收敛速度；或者将 `x2 / x3 / x4` 三个尺度的结果分别画成柱状图，从而更直观地展示倍率增加带来的性能下降，等等。

### 4.5 图像结果与局部细节分析

**由于当前阶段我们还是以文字分析为主，因此图像结果部分先保留现有图片和占位。后续如需提交更完整版本，可继续补入模型输出图、Bicubic 基线和局部细节放大图。**

#### 4.5.1 原始示例图

下面先给出当前仓库中用于演示的原始图像示例：

![Demo 原图 01](demo/original/01.png)

![Demo 原图 02](demo/original/02.png)

#### 4.5.2 重建结果占位

**以下位置预留给三种模型的重建结果图，后续可由队友补充生成后的 SR 图：**

![SRCNN x2 重建结果](fig_srcnn_x2_result.png)

![ESPCN x2 重建结果](fig_espcn_x2_result.png)

![EDSR x2 重建结果](fig_edsr_x2_result.png)

#### 4.5.3 局部细节放大图占位

**助教评分细则中特别强调了局部区域的放大对比，因此我们建议后续在最终版中加入如下内容：

1. 原图 HR 局部；
2. Bicubic 放大结果局部；
3. 三种模型输出的对应局部；
4. 对边缘、纹理和伪影的简要分析。**

当前先保留占位：

![局部细节对比图 1](detail_compare_1.png)

![局部细节对比图 2](detail_compare_2.png)

从现有实验结果趋势看，我们预期 `EDSR` 在纹理恢复和边缘清晰度上会更占优势，而 `SRCNN`、`ESPCN` 则更适合作为基准参照。后续若把图补齐，这部分也会成为报告中比较有说服力的部分。

### 4.6 结果记录说明

本次项目中，我们最终主要保留了 `metrics.png` 和 `.log` 文件，其能够支持 PJ 结果的展示。

1. 曲线图可以直接展示训练与验证趋势；
2. 日志文件中明确记录了训练参数和最终最佳 `PSNR`；
3. 模型权重 `best.pt` 已全部保留，保证后续仍可继续补测和补图。

因此综上所述，我们认为项目主体已经具备了完整的材料基础与分析，可作为一个最终落地的结果。

---

## 5. 实验分工

本次项目由三人协作完成，整体分工如下：

1. 庞嘉
   - 负责实验环境配置，完成核心代码框架的搭建与主要模型训练；
2. 朱程炀
   - 参与模型实现、代码检查与实验流程梳理，协助完成主要模型的训练与测试；
3. 郭诣丰
   - 分析不同模型与不同倍率下的表现差异，负责测试分析总结与报告撰写；

在整个实验过程中，我们三人保持了持续沟通，没有一位同学懈怠、偷懒。虽然各自分工有所侧重，但从整体上看，项目推进、结果核对和报告整理都是在共同讨论基础上完成的。

---

## 6. 收获与感想

在序言课上选题时，我们还对超分辨率的具体实现方法不甚了解，感觉这个任务比较有意义，就决定选择这个项目。在后续学习、实现的过程中，我们才慢慢理解了其中的知识，也意识到课堂上听懂一个模型的基本原理，和真正把一整套实验流程搭起来，感受还是有所不同的。比如在课上，我们更多接触的是卷积、上采样这些相对独立的知识点；但在项目中，数据退化的构造、训练过程的记录、验证指标的计算等等，都是必须认真处理的问题。

此外，我们也更加具体地体会到了模型结构设计和现实工程之间的关系。单看论文时，我们往往容易只是粗暴地关注哪个模型更强；但在真正动手后，我们慢慢发现，经典模型之所以经典，不仅因为它们有代表性的性能，也因为它们各自对应了不同的思考方向。`SRCNN` 让我们看到了端到端超分辨率最早期、最清楚的范式；`ESPCN` 让我们理解了为什么把计算留在 LR 空间是有意义的；`EDSR` 则提醒我们，更深的残差结构在图像恢复任务里确实能够带来稳定提升。把这三者放在一起比较，本身就是很有收获的一件事。

在一步步完成的过程中，由于有 AI 的辅助，实际上核心的代码实现并不是占用最多时间的部分。在实验前后，我们都花了不少时间编辑查阅论文、对照原始设计、分析究竟采用什么配置，这反而是更有收获的过程。经过这个 PJ，我们对单幅图像超分辨率的问题有了更完整的认识，同时也学到了不少 CV 的基本原理。这也为后续继续学习更复杂的图像恢复和生成与增强方法打下了基础。
